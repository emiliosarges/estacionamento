spring:
  application:
    name: estacionamento
  datasource:
    url: jdbc:postgresql://localhost:5433/estacionamento
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true